# Copy this file to `.env` and fill in your values.
# Docker Compose automatically loads `.env` when present.

# Optional: override the exposed Streamlit port on the host (defaults to 8501).
HOST_STREAMLIT_PORT=8501

# Optional: override the in-container server port (defaults to 8501).
STREAMLIT_SERVER_PORT=8501

# Kaggle API credentials (https://www.kaggle.com/settings/account)
KAGGLE_USERNAME=your_kaggle_username
KAGGLE_KEY=your_kaggle_api_key

# Optional: override the Kaggle config directory (container path).
# KAGGLE_CONFIG_DIR=/root/.kaggle
